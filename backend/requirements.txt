annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.0
click==8.4.1
colorama==0.4.6
et_xmlfile==2.0.0
fastapi==0.138.0
h11==0.16.0
idna==3.18
openpyxl==3.1.5
pydantic==2.13.4
pydantic_core==2.46.4
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.49.0
pyjwt==2.13.0
requests==2.34.2
python-dotenv==1.2.2
supabase==2.31.0
reportlab==4.4.10
apscheduler==3.11.3
gspread==6.2.1
slowapi==0.1.10

# NOTE: this is a starting pin, not gospel — run this whole backend
# locally + on a staging Render instance once, confirm nothing broke,
# then `pip freeze > requirements.txt` to lock in the exact versions
# you actually tested against before shipping to production.
#
# Also: the old file listed a package literally named "dotenv" — that's
# a different, unmaintained PyPI package that happens to share an import
# name with the real one. "python-dotenv" (above) is the one your code
# actually wants (`from dotenv import load_dotenv`). Worth confirming
# your current venv has the right one installed:
#   pip show dotenv python-dotenv
